export type TaskType = {
    title: string,
    description: string,
    dueDate: string,
    completed: boolean,
}



export type TaskForm = {
    title: string,
    description: string,
    dueDate: string,
    completed: boolean
}